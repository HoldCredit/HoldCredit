1. 테이블(엔티티) 생성
  - jpa작업 중이니까 entity 생성
  - myBatis라면 create sql로 테이블 생성

2. 테이블의 컬럼 순서와 엑셀파일 컬럼 순서 일치 시킬것.

3. sqldeveloper에서 계정에 들어가서 엑셀과 매칭되는 테이블 우클릭 > 데이터 임포트 클릭

4. 파일 불러오기 csv로 (xlsx는 렉걸림), UTF-8 설정

5. 다음다음다음 끝까지 클릭하면 끝